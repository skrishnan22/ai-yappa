lint-staged
if ! command -v gitleaks >/dev/null 2>&1; then
	echo "gitleaks is not on PATH. Install it with: brew install gitleaks"
	echo "https://github.com/gitleaks/gitleaks#installing"
	exit 1
fi
npm run gitleaks:staged
