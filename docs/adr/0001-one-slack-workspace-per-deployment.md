# Bind each deployment to one Slack workspace

Each Slack Agent Deployment serves exactly one Slack Workspace. This accepts per-workspace deployment overhead in exchange for simpler isolation, credential ownership, routing, and failure containment than a shared multi-tenant service.
